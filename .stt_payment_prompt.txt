Per-use Google STT payment + UI wiring for lazyuniv-ai. Working dir: /c/Users/김준현/meeting-app. Repo: jhyunkim35-cloud/lazyuniv-ai.

CONTEXT:
A sister sub-claude is building /api/google-stt.js (Chirp_2 Korean STT, paid per-use). Your job is the payment flow + UI:

1. Toss payment integration for per-use STT entitlement
2. Recorder UI: option toggle ("기본 STT 무료" vs "Google STT (정확도 높음, 유료)")
3. Estimate audio length BEFORE recording finishes, calculate price tier
4. Pay via Toss → entitlement created in Firestore → STT proceeds with new endpoint
5. Default flow (free AssemblyAI) stays unchanged

═══ PRICING ═══

30분 단위 결제, 500원 마진:
- 0~30분 → 1,500원 (entitlement.minutes = 30)
- 31~60분 → 2,500원 (entitlement.minutes = 60)
- 61~90분 → 3,500원 (entitlement.minutes = 90)
- 91~120분 → 4,500원 (entitlement.minutes = 120)
- 121~150분 → 5,500원 (entitlement.minutes = 150)
- 151~180분 → 6,600원 (entitlement.minutes = 180)
- (계속: +1,000원 per +30min)

Helper function: priceFor(audioMinutes) → {minutes: roundedUp30, priceKRW}

═══ Toss integration ═══

api/toss.js already handles plan purchases (monthly/single). Extend it (or add a new branch) to handle 'sttEntitlement' purchases.

When user requests STT entitlement payment:
1. Client estimates audio length, calls /api/toss with action='confirm', kind='sttEntitlement', minutes, amount
2. Server (api/toss.js) confirms with Toss, on success creates Firestore record:
   users/{uid}/sttEntitlements/{paymentKey}: {
     minutes,
     priceKRW,
     paidAt: serverTimestamp,
     consumed: false,
     consumedAt: null,
     transcriptId: null,
     orderId,
     paymentKey,
   }

Use paymentKey as document id for idempotency (Toss webhook retries won't double-create).

The Toss flow itself (popup, payment widget) is already wired in payment.js. Add a new function callable from recorder.js: payForSttEntitlement(audioMinutes) that returns a Promise resolving when payment confirmed (Firestore entitlement created).

═══ Recorder UI changes ═══

In recorder.js, add an STT engine selector visible right before recording starts (or right after recording stops, before transcription):

```
STT 엔진 선택:
( ) 기본 (AssemblyAI) - 무료, 빠름, 정확도 보통
( ) Google STT (Chirp_2) - 1,500원~, 한국어 정확도 최상
```

Default = AssemblyAI (free).

When user selects Google STT:
1. After recording stops, calculate audio duration in minutes (modalState.recordingDurationSec / 60, round up)
2. Compute priceFor(minutes)
3. Show confirmation modal: "이 강의는 N분입니다. Google STT 변환 비용은 X원입니다. 결제하시겠습니까?"
4. On confirm: call payForSttEntitlement(minutes) → Toss popup → on success → continue with /api/google-stt instead of /api/assemblyai
5. On payment fail/cancel: fall back to /api/assemblyai (free) automatically with toast "결제 취소 — 기본 STT로 진행합니다"

═══ Endpoint switch ═══

In handleAudioBlob(blob, filename), after upload, branch on selected engine:

```js
const engine = modalState.sttEngine || 'assemblyai';  // default
if (engine === 'google') {
  // user already paid — call /api/google-stt?action=transcribe
  ...
} else {
  // existing path
  ...
}
```

The status polling already works for both endpoints (response shape compatible per the google-stt sub-claude spec).

═══ Same flow for .txt upload ═══

When user uploads pre-existing .txt (BYO Clova/Daglo path), no engine selection or payment — just plain rec slot fill (already works). The engine selector ONLY appears when user is in the recording flow.

═══ CONSTRAINTS ═══

- Edit api/toss.js (add sttEntitlement kind), public/js/payment.js (payForSttEntitlement), public/js/recorder.js (engine selector + branch + payment flow)
- DO NOT touch api/google-stt.js (sister sub-claude is building it). Reference its expected interface only.
- DO NOT touch firestore.rules (server uses Admin SDK which bypasses rules).
- Single commit. Message: "feat(stt): per-use Google STT payment flow — 30min tiers, 500원 fixed margin, Toss integration"
- Push to origin main; if rejected, git pull --rebase and retry.

═══ VERIFY ═══

- node --check
- Mentally walk through:
  - User starts recording → 1.5hr lecture → stops → engine selector appears (default: AssemblyAI)
  - User picks Google STT → confirmation: "90분 강의, 3,500원" → Toss popup → pays → entitlement created → /api/google-stt called → STT proceeds
  - Or user picks default → AssemblyAI free path (existing flow)
  - .txt upload (BYO) → no engine selector, just slot fill

═══ OUTPUT ═══

Last line of your response, exactly one of:
  DONE: <commit_hash> | per-use STT payment flow live
or:
  FAILED: <reason>
