STT background floating pill for lazyuniv-ai. Working dir: /c/Users/김준현/meeting-app. Repo: jhyunkim35-cloud/lazyuniv-ai.

CONTEXT:
recorder.js already has a floating pill for the RECORDING phase (when MediaRecorder is capturing — small draggable widget at bottom-right with timer + pause/stop/expand buttons).

But during the STT phase (after recording stops, while AssemblyAI processes the audio — can take 30 minutes for a 90-min lecture), if the user clicks "창 닫기 (백그라운드 진행)" the modal hides entirely. The pipeline keeps running in JS memory but the user has zero visibility into:
- which stage is active (queued / processing / completed)
- elapsed time so far
- when it'll finish

Currently the only feedback when STT completes is a final toast. Users need a persistent visual indicator while STT is in flight.

═══ DELIVERABLE ═══

Extend the existing recorder pill to ALSO surface during the STT (transcribing) phase. Reuse the same draggable floating pill component — just swap its content based on `modalState.phase`.

═══ Behavior ═══

1. **When user clicks "창 닫기 (백그라운드 진행)" on the STT screen** (currently calls hideModal):
   - Don't fully hide. Switch to a minimized STT pill instead (similar to how the recording pill already minimizes via minimizePill()).
   - Pill content while STT is running:
     - Small spinner icon
     - Stage label (대기 중 / 변환 중 / 마무리 중)
     - Elapsed time MM:SS
     - Expand button (click → full STT screen back)
     - NO stop button (STT can't be cancelled mid-flight cleanly — show only expand)

2. **Pill styling for STT** (separate visual from recording pill):
   - Background: subtle blue or neutral (NOT red — recording uses red dot, STT is different state)
   - Same width (~280px), same drag behavior, same persistence of position via localStorage
   - Reuse the existing pill DOM shell; just toggle visible content based on phase
   - Spinner: same CSS spinner as the inline STT screen (`.rec-stt-stage-dot--spinner`) but smaller, 14px diameter

3. **State transitions**:
   - During STT processing: pill shows "변환 중… 03:24" with spinner
   - When poll() reports status='completed': pill auto-expands to show the success/done screen so the user sees the result
   - When poll() reports status='error': pill auto-expands to show the error screen
   - Long-running warning (>5min): pill shows yellow tint + "긴 강의는 시간이 더 걸립니다" tooltip on hover

4. **Edge cases**:
   - If user expands during STT, then minimizes again → pill should re-render correctly
   - If user starts a NEW recording while a previous STT is still in flight → block the new recording with a toast ("이전 변환이 끝난 후 시작하세요"). This is rarely going to happen but worth defending.
   - If the user reloads the page mid-STT, transcript is lost (already-known limitation — don't try to fix in this commit, just keep the existing behavior).

═══ Implementation hints ═══

In recorder.js:
- The pill DOM is already present. Look at `injectPillStyles()` and the pill DIV rendered inside the modal HTML.
- Current pill content: `#recPillDot, #recPillTimer, #recPillPauseBtn, #recPillStopBtn, #recPillExpandBtn`
- Add a SECOND pill content variant (e.g. inside the same panel, toggle visibility based on phase). Or alternately, swap innerHTML when entering STT minimized mode.
- The "창 닫기 (백그라운드 진행)" button currently calls `hideModal`. Change it (only during STT phase) to call `minimizePill()` — but the pill's appearance must reflect the STT phase.
- Update `updatePillUI()` to handle phase==='transcribing' (or whatever the STT phase is set to in `handleAudioBlob`).
- Keep the recording pill flow intact — don't break anything.
- Mirror the elapsed time + stage label from inside the polling loop, similar to how the timer mirrors to `recPillTimer`.

═══ CONSTRAINTS ═══

- Edit ONLY /public/js/recorder.js. No other files.
- Single commit. Message: "feat(recorder): persistent STT progress pill — visibility while transcribing in background"
- Push to origin main; if rejected, git pull --rebase and retry.
- Don't touch any other functionality (recording, file upload, transcripts_store integration).
- Verify with node --check before committing.

═══ Output ═══

Last line of your response, exactly one of:
  DONE: <commit_hash> | STT pill live
or:
  FAILED: <one-line reason>
