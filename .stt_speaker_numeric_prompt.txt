STT speaker labeling redesign for lazyuniv-ai. Working dir: /c/Users/김준현/meeting-app. Repo: jhyunkim35-cloud/lazyuniv-ai. Vercel auto-deploys on push to main.

CONTEXT:
The current STT path uses AssemblyAI with `speaker_labels: true`. AssemblyAI returns utterances tagged "A", "B", "C", … in arbitrary order. The operator wants:

1. Speakers are labeled with numbers ("발화자 1", "발화자 2", …) instead of letters
2. The speaker who spoke the MOST (by total duration or word count) is labeled 1, the next most is 2, etc. So "발화자 1" is always the dominant speaker — usually the lecturer.

YOUR JOB:
Find where AssemblyAI's status response is returned to the client (api/assemblyai.js, action=status path). Currently it likely returns `j.text` or `j.utterances` raw. Add post-processing:

1. Compute speaker totals
   - Iterate j.utterances (each has `speaker` letter, `text`, `start`, `end`, optional `words`)
   - For each unique speaker letter, sum (end - start) → totalMs
   - Tiebreaker: word count
2. Build a remap: sorted speakers by totalMs desc → speaker A(letter) → number starting at 1
3. Rebuild the transcript text:
   - For each utterance, prefix with "발화자 N: " (where N is the remapped number)
   - One utterance per line, blank line between speaker changes for readability
   - If the response had a flat j.text without utterances, leave it untouched and skip remapping
4. Return:
   - text: the rebuilt numbered transcript (overrides j.text)
   - utterances: optionally include remapped utterances [{speaker: 1, text, start, end}, ...]
   - speaker_count: number of distinct speakers

EDGE CASES:
- Single speaker (1 unique letter) → label everything "발화자 1: ..." (still useful — confirms it's monologue)
- No utterances field (older AssemblyAI response) → return text as-is, log a warning
- Empty text → return as before
- Make sure the response stays JSON-shaped, status: 'completed' etc.

CONSTRAINTS:
- Edit ONLY /api/assemblyai.js. Don't touch recorder.js, transcripts_store.js, or any other file.
- Preserve all existing behavior (Bearer token verification, rate limit, polling, error paths).
- Single commit. Message:
    feat(stt): number speakers by speaking time — dominant speaker = 1
- Push to origin main; if rejected, git pull --rebase and retry.
- Sister sub-claudes are touching other files (recorder.js, etc.) — your edit is API-only so no conflict expected.

VERIFY:
- node --check /api/assemblyai.js
- Read your own diff
- Mentally walk through:
  - 3 speakers, A=120s B=300s C=200s → remap: B→1, C→2, A→3
  - Output starts with "발화자 1: " for all B utterances
  - 1 speaker A → all "발화자 1: " prefix
  - No utterances → text unchanged

OUTPUT (LAST line of your response):
  DONE: <commit_hash>
or:
  FAILED: <one-line reason>
