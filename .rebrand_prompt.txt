Full rebrand: Lazyuniv AI → Notyx. Working dir: /c/Users/김준현/meeting-app. Repo: jhyunkim35-cloud/lazyuniv-ai. Vercel auto-deploys on push to main.

CONTEXT:
The product name is changing from "Lazyuniv AI" / "Lazyuniv" / "유니브 AI" to "Notyx". This is a user-facing rename only — Firebase project ID, Storage URLs, and Firestore document paths stay as 'lazyuniv-ai' (those are internal infra IDs that can't be changed without data migration). The GitHub repo + Vercel project rename will be handled by the operator later.

═══ SCOPE ═══

Rename all USER-FACING mentions of the old brand to Notyx. Find them via grep:

Old brand patterns (find any case):
- "Lazyuniv AI"  /  "Lazyuniv-AI"  /  "lazyuniv ai"
- "Lazyuniv"     /  "lazyuniv"      (standalone, not part of lazyuniv-ai infra ID)
- "유니브 AI"   /  "유니브AI"      /  "유니브"  (Korean)
- "lazyuniv-ai.vercel.app"  (this domain in user-facing copy — keep in actual code if used as fetch URL, but replace in display text)

New brand:
- "Notyx"  (English, capitalized)
- "notyx"  (lowercase, e.g. domain or class names)
- "노틱스"  (Korean)

═══ CRITICAL: WHAT TO LEAVE ALONE ═══

Firebase + infrastructure IDs MUST stay as 'lazyuniv-ai' to avoid breaking the live database/storage:

DO NOT TOUCH:
- public/firebase config object: projectId, storageBucket, messagingSenderId, appId, etc.
- firebaseAppId / firebase-adminsdk service account email
- Firebase Storage URLs (lazyuniv-ai.firebasestorage.app or similar)
- Firestore project IDs in .firebaserc, firebase.json
- firestore.rules (just rule logic, no user-facing strings to update)
- storage.rules
- vercel.json
- Any GitHub URL (github.com/jhyunkim35-cloud/lazyuniv-ai) — operator will rename repo later
- api/*.js internal logging (server logs aren't user-facing — leave them as-is for now)
- package.json "name" field (will be renamed during repo rename)
- Audit comments inside firestore.rules / source code that reference past commit context

═══ FILES TO UPDATE ═══

Find user-facing brand mentions in:

1. /public/index.html — biggest one. Update:
   - <title>...</title>
   - <meta name="description">
   - <meta property="og:title">, og:description, og:site_name
   - <meta name="twitter:title">, twitter:description
   - Sidebar logo text (look for "Lazyuniv AI" or similar near logo SVG)
   - Top bar / header brand text
   - Footer copyright + brand mention
   - Any onboarding/empty-state text mentioning the brand
   - Welcome / loading / 404 / error screens

2. /public/manifest.json — PWA manifest:
   - "name": "Lazyuniv AI" → "Notyx"
   - "short_name": "Lazyuniv" → "Notyx"
   - "description"
   - Theme/background colors stay

3. /public/terms.html — legal:
   - Replace product name throughout
   - "본 서비스('Lazyuniv AI')" → "본 서비스('Notyx')"
   - Keep placeholder fields as they are (operator name, address, biz reg no.)

4. /public/privacy.html — same pattern

5. /public/admin.html — admin dashboard:
   - Title bar and header text → Notyx
   - It's operator-facing but consistency matters

6. All /public/js/*.js files — search for user-facing strings:
   - Toast messages mentioning the brand
   - Modal headers
   - Onboarding hints
   - Footer text constructed dynamically
   - Notification copy

7. README.md if exists

═══ PROCESS ═══

1. First do a dry run grep to find all mentions:
   grep -rni "lazyuniv\|유니브 AI\|유니브AI" /c/Users/김준현/meeting-app/public /c/Users/김준현/meeting-app/api /c/Users/김준현/meeting-app/README.md 2>/dev/null
   
2. Categorize results:
   - User-facing (change)
   - Infra (leave)
   - Borderline (e.g. server logs — leave for now)

3. Make changes file by file. Use str_replace for surgical edits.

4. For each change, verify:
   - Korean: "유니브 AI" → "노틱스"  (note: full-width space, not half-width)
   - English: "Lazyuniv AI" → "Notyx"
   - Lowercase: "lazyuniv" → "notyx" (only when not infra)

5. After all edits, run the grep again to confirm no user-facing brand mentions remain.

6. Single commit, message:
     rebrand: Lazyuniv AI → Notyx (user-facing only; infra IDs unchanged)
   With multi-line body explaining what changed and what stayed.

7. Push to origin main; if rejected, git pull --rebase and retry.

═══ EXAMPLES ═══

Before:
   <title>Lazyuniv AI · 강의 자료를 학습 노트로</title>
After:
   <title>Notyx · 강의 자료를 학습 노트로</title>

Before:
   showToast('Lazyuniv AI에 로그인해 주세요');
After:
   showToast('Notyx에 로그인해 주세요');

Before (LEAVE — Firebase config):
   const firebaseConfig = {
     projectId: 'lazyuniv-ai',
     storageBucket: 'lazyuniv-ai.firebasestorage.app',
     ...
   };
After:
   (unchanged)

Before:
   <h1>유니브 AI</h1>
After:
   <h1>노틱스</h1>

═══ VERIFY BEFORE COMMIT ═══

Run after changes:
   grep -rni "lazyuniv ai\|lazyuniv-ai\b\|유니브 AI\|유니브AI\|lazyuniv\b" /c/Users/김준현/meeting-app/public /c/Users/김준현/meeting-app/api 2>/dev/null

The remaining hits should ALL be infra/config (Firebase project ID, storage bucket, repo URL, server logs). Document the categorization in your commit message body.

═══ OUTPUT ═══

Last line of your response, exactly one of:
  DONE: <commit_hash> | rebrand to Notyx complete (X user-facing mentions changed, Y infra mentions preserved)
or:
  FAILED: <one-line reason>
