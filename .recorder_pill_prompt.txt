Make the recorder modal at /c/Users/김준현/meeting-app/public/js/recorder.js into a draggable floating widget that lets users work on other parts of the app while recording continues in the background.

UX SPEC:
1. Recording starts as it does today (full-screen modal with timer/meter/buttons)
2. Add a NEW "최소화" button (lucide icon: minimize-2 or chevron-down) in the live screen header
3. When minimized:
   - The backdrop (.recorder-backdrop) becomes transparent and click-through (pointer-events: none)
   - The panel (.recorder-panel) shrinks to a compact pill (~280px wide) and floats at the bottom-right of the viewport by default
   - Compact pill shows: recording dot (red, blinking) + timer + ⏸ pause/resume button + ⏹ stop button + ⬜ expand button
   - Pill is DRAGGABLE: mousedown on the pill (excluding buttons) → drag to move. Save position to localStorage so it persists between sessions.
   - Pill stays on top of the rest of the app (z-index 10000+)
   - The user can scroll, click folders, type in inputs, navigate, etc. — recorder runs in the background
4. When expanded again (via expand button):
   - Backdrop returns to full opacity, pointer-events restored
   - Panel returns to full size + center
   - State preserved (timer keeps counting, mic stream untouched)
5. ESC key while minimized: do nothing (avoid accidental cancel)
6. Closing while minimized = same closeModalIfSafe behavior (confirm if recording, just hide if idle)

IMPLEMENTATION CONSTRAINTS — VERY IMPORTANT:
- Edit ONLY /public/js/recorder.js. Do NOT touch /public/index.html (sister sub-claudes are working on it for legal page footer links).
- All CSS must be added by INJECTING a <style> tag into document.head from inside recorder.js — DO NOT add a <link rel="stylesheet"> to index.html.
- Existing styles for .recorder-modal / .recorder-backdrop / .recorder-panel may live in index.html or a stylesheet — leave them alone, just override with more-specific selectors (.recorder-modal--minimized .recorder-panel, etc.) injected from JS.
- Use the same design tokens (--surface, --primary, --text, 12px radius) — read them from existing styles.

DRAG LOGIC:
- Track mousedown on the pill (not its buttons).
- On mousemove: translate3d() the pill, clamping to viewport bounds (don't let it disappear off-screen).
- On mouseup: localStorage.setItem('recorder.pillPos', JSON.stringify({x, y})).
- On next minimize: read back localStorage and apply.
- Touch events too (touchstart/touchmove/touchend) for mobile/tablet.

DON'T BREAK:
- Timer math (we just fixed it — phase='recording' must be preserved through minimize/expand).
- Audio meter (the bar updating).
- Recording chunks accumulation.
- closeModalIfSafe / cancelLiveRecording flow.
- Pause/resume.
- Phase 4 lucide icons in the modal (mic, x, check, alert-triangle).

WHEN TO MINIMIZE AUTOMATICALLY:
- Add this UX nicety: while phase==='recording' OR 'paused', if the user clicks the backdrop (which is what currently triggers closeModalIfSafe → confirm dialog), AUTO-MINIMIZE instead of confirm. Idle / done / error / upload screens keep the original close confirmation.
- This solves the bug user reported: "녹음 중에 화면 다른 곳을 클릭하면 녹음이 사라지는 것 같다." Now the backdrop click during recording smoothly minimizes instead.

CONSTRAINTS:
- Don't touch /api/claude.js (B1 strict mode sub-claude is in there)
- Don't touch firestore.rules (rules audit sub-claude is there)
- Don't touch /public/index.html (legal pages sub-claude adds footer links there)
- Don't create new files outside what's strictly needed (recorder.js edit is enough; if you really must, add /public/js/recorder-pill.js and import via dynamic script tag from recorder.js — but inline edits are preferred)
- Single descriptive commit with a clear "feat: floating recorder pill" subject
- Push to origin main; if push rejected do `git pull --rebase origin main` and retry

VERIFY:
- node --check /public/js/recorder.js
- Read your own diff
- Mentally walk through: start recording → minimize → click around app → expand → stop → confirm transcript still saves correctly

Output the LAST line as exactly:
  DONE: <commit_hash>
or:
  FAILED: <one-line reason>
