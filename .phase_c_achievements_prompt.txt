Achievement / Badge System for lazyuniv-ai. Working dir: /c/Users/김준현/meeting-app. Repo: jhyunkim35-cloud/lazyuniv-ai.

CONTEXT: Phase A (SRS review UI) and Phase B (daily goal + streak calendar) just shipped. Now add Duolingo-style achievements that fire dopamine.

═══ Part 1: Achievement definitions ═══

Create /public/js/achievements.js. Define a static catalog (constant array) of milestones:

```js
const ACHIEVEMENTS = [
  // Onboarding
  { id: 'first_note',     icon: 'sparkles',     title: '첫 노트',         desc: '첫 노트를 생성했어요', condition: s => s.totalNotes >= 1 },
  { id: 'first_review',   icon: 'check',        title: '첫 복습',         desc: '첫 SRS 카드를 복습했어요', condition: s => s.totalReviews >= 1 },
  { id: 'first_quiz',     icon: 'graduation-cap', title: '첫 퀴즈',       desc: '첫 퀴즈를 풀었어요', condition: s => s.totalQuizzes >= 1 },

  // Streak
  { id: 'streak_3',       icon: 'flame',        title: '3일 연속',        desc: '3일 연속 학습', condition: s => s.streakDays >= 3 },
  { id: 'streak_7',       icon: 'flame',        title: '7일 연속',        desc: '일주일 연속 학습', condition: s => s.streakDays >= 7 },
  { id: 'streak_30',      icon: 'flame',        title: '30일 연속',       desc: '한 달 연속 학습', condition: s => s.streakDays >= 30 },
  { id: 'streak_100',     icon: 'flame',        title: '100일 연속',      desc: '백일 연속 학습', condition: s => s.streakDays >= 100 },

  // XP
  { id: 'xp_100',         icon: 'star',         title: '100 XP',          desc: '100 XP 달성', condition: s => s.xpTotal >= 100 },
  { id: 'xp_500',         icon: 'star',         title: '500 XP',          desc: '500 XP 달성', condition: s => s.xpTotal >= 500 },
  { id: 'xp_1000',        icon: 'star',         title: '1,000 XP',        desc: '1,000 XP 달성', condition: s => s.xpTotal >= 1000 },
  { id: 'xp_5000',        icon: 'star',         title: '5,000 XP',        desc: '5,000 XP 달성', condition: s => s.xpTotal >= 5000 },

  // Volume
  { id: 'reviews_50',     icon: 'check',        title: '50장 복습',       desc: '카드 50장 복습 완료', condition: s => s.totalReviews >= 50 },
  { id: 'reviews_500',    icon: 'check',        title: '500장 복습',      desc: '카드 500장 복습 완료', condition: s => s.totalReviews >= 500 },
  { id: 'notes_10',       icon: 'book-open',    title: '10개 노트',       desc: '노트 10개 생성', condition: s => s.totalNotes >= 10 },
  { id: 'notes_50',       icon: 'book-open',    title: '50개 노트',       desc: '노트 50개 생성', condition: s => s.totalNotes >= 50 },

  // Quality
  { id: 'perfect_quiz',   icon: 'sparkles',     title: '만점 퀴즈',       desc: '퀴즈 만점 달성', condition: s => s.perfectQuizzes >= 1 },
  { id: 'goal_streak_7',  icon: 'target',       title: '목표 일주일',     desc: '7일 연속 목표 달성', condition: s => s.goalStreak >= 7 },
];
```

Functions:
- checkAchievements(state) — iterate catalog, return any newly-unlocked (not in state.unlockedIds yet)
- unlockAchievement(id) — add to state.unlockedIds, persist via saveGamificationState, fire celebration UI
- renderAchievementGrid(parent) — render entire catalog as grid, locked vs unlocked styling

═══ Part 2: Extend gamification state ═══

Add fields to gamification state schema (extend existing in gamification.js):
- totalNotes (incremented in markNoteCreated from Phase B)
- totalReviews (incremented in onCardReviewed)
- totalQuizzes (incremented in markQuizCompleted)
- perfectQuizzes (incremented when quiz score == 100%)
- goalStreak (consecutive days of goal completion — derived from dailyHistory)
- unlockedIds (array of achievement IDs unlocked)

Make sure pre-existing users without these fields default to 0/[].

═══ Part 3: Trigger checks ═══

After every state change that could unlock an achievement, call checkAchievements + unlockAchievement for each new one. Hook points:
- onCardReviewed: after state save → check
- markNoteCreated: after state save → check
- markQuizCompleted: after state save → check
- daily goal completion (Phase B): after celebration → check

Don't double-unlock.

═══ Part 4: Achievement unlock UI ═══

When unlocked, show a toast or modal:
- Lucide icon (large, with --primary glow)
- "🏆 업적 달성: <title>"
- Description
- "확인" button or auto-dismiss after 4s

Animation: bounce-in from top, gold sparkle effect.

═══ Part 5: Achievement Grid Page ═══

Add a "내 업적" section to home view (collapsible or a separate sub-page):
- Grid of all achievements (4-5 per row on desktop, 3 on mobile)
- Each cell: lucide icon + title
- Locked: grayscale + "잠금" overlay
- Unlocked: full color + "달성일" tooltip
- Progress count: "X / Y 달성" at the top

═══ Constraints ═══

- File scope:
  - NEW: /public/js/achievements.js
  - EDIT: /public/js/gamification.js (extend state + add hook calls)
  - EDIT: /public/js/home_view.js (add grid section)
  - EDIT: /public/js/quiz.js (track perfect score → state.perfectQuizzes)
  - EDIT: /public/index.html (add script tag for achievements.js, in load order after gamification.js, before main.js)
- DO NOT touch: api/*, firestore.rules, storage.rules, vercel.json, recorder.js, payment.js, srs.js
- Single commit, message: "feat(gamif): achievement/badge system with 16 milestones + unlock celebration"
- Push to origin main; if rejected, git pull --rebase and retry

═══ Verify ═══

- node --check
- Read diffs
- Walk through:
  - New user generates first note → "첫 노트" achievement unlocks → toast shows
  - User reviews 50 cards over time → "50장 복습" unlocks
  - 7-day streak → "7일 연속" unlocks (note: should NOT re-unlock if user breaks streak then recovers; once unlocked, stays unlocked)
  - Achievement grid shows progress: "5 / 16 달성"

═══ Output ═══

Last line of your response, exactly one of:
  DONE: <commit_hash> | achievement system live
or:
  FAILED: <one-line reason>
