node_modules/
.env
.env.local
.env.*.local
.vercel/

# editor / OS noise
.DS_Store
*.swp

# old html backups (already in repo but stop adding new ones)
index_backup*.html
index_new.html

# sub-claude prompt files (operator-only scratch, not product code)
.*_prompt.txt

.vercel
.env*.local

# U7b diarization worker — local venv + scratch audio, not product code
worker/.venv/
worker/*.wav
worker/serviceAccount.json
__pycache__/
