Phase 4-A: Replace emojis with Lucide icons in newNoteView only.

Region: /public/index.html lines ~4002 to ~4262 (the div id="newNoteView" block, ends right before splitViewer at line 4263).

Mapping (only emojis appearing inside newNoteView):
  📊 → presentation
  🎙️ → mic
  🎙  → mic
  ✓  → check
  📓 → notebook-pen
  💡 → lightbulb
  📁 → folder
  ✨ → sparkles
  ℹ️ → info
  🎯 → target
  📦 → package

Rules:
- Use class="icon-sm" for inline icons next to text. No class for large hero icons (e.g. the 📊 inside upload-icon-wrap is hero — no class).
- Replace bare emoji glyphs with <i data-lucide="NAME"></i> markup. Preserve all surrounding HTML structure, IDs, classes, event handlers.
- Toast emojis inside showToast() string literals are intentional — DO NOT TOUCH (those live in JS files, not HTML).
- Do not modify CSS or layout. Just emoji-to-lucide swap.
- Single commit, push to origin main.

Output the LAST line of your response as exactly one of:
  DONE: <commit_hash> | <icons> icons replaced
  FAILED: <one-line reason>
